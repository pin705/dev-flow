{
  "name": "@diffmint/cli",
  "version": "0.1.1",
  "private": true,
  "bin": {
    "dm": "./bin/dm"
  },
  "type": "module",
  "scripts": {
    "dev": "node --import tsx src/index.ts --help",
    "build:npm": "node ./scripts/build-npm-package.mjs",
    "pack:npm": "pnpm build:npm && cd dist/npm && npm pack"
  },
  "dependencies": {
    "@diffmint/contracts": "workspace:*",
    "@diffmint/review-core": "workspace:*"
  }
}
