---
title: Install the CLI
description: Set up the Devflow terminal workflow and connect it to the control plane.
section: Getting Started
order: 20
surfaces:
  - cli
  - admin
related:
  - /docs/cli/reference
  - /docs/concepts/local-first-review
---

# Install the CLI

The CLI is the canonical entry point for reviews, explain flows, test generation, history, and diagnostics.

## Install

```bash
pnpm install
```

## Sign in and select a workspace

```bash
devflow auth login
devflow config set-provider qwen
devflow doctor
```

## Typical review commands

```bash
devflow review --staged
devflow review --base origin/main
devflow review --files src/app/page.tsx src/lib/review.ts
devflow explain src/app/page.tsx
devflow tests src/lib/review.ts
```

See the full [CLI reference](/docs/cli/reference) for flags and JSON output.

For containerized local development with hot reload, see [Docker development](/docs/getting-started/docker-development).
